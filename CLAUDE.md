# CODEBASE.md — Tippi

> **First-read map for AI coding agents.** Read this before grep/ls when starting work on Tippi. Keeps you grounded so you don't spend tokens re-discovering the repo every session.
>
> Last updated: 2026-05-24 (v1.8.1)

## What this is

System-wide AI writing assistant for macOS. Select text in any app → hotkey (⌥⌘T) → AI transforms → result back in the original app. Also dictation mode (⌃⌥⌘M): hotkey → speak → Whisper transcribes → inserted at cursor. Optional LLM polish for dictation transcripts.

Open source, MIT, Developer ID signed, Sparkle auto-updates. BYOK with 8 providers (6 cloud + 2 local).

## Tech

| Layer | Choice |
|---|---|
| Language | Swift 6.x |
| UI | SwiftUI + AppKit bridges |
| Min macOS | 15.0 Sequoia |
| Arch | Apple Silicon only (arm64) |
| Build | XcodeGen (`project.yml`) + Makefile |
| Sandbox | OFF (needed for cross-app text capture via Accessibility API) |
| Signing | Developer ID + Hardened Runtime + Notarization |
| Distribution | Custom DMG + Sparkle, no App Store |
| Deps | Sparkle 2 (SPM) — only third-party dep |

## Entry points

- `Tippi/App/TippiApp.swift` — `@main` SwiftUI App
- `Tippi/App/AppDelegate.swift` — lifecycle, hotkey registration, menubar, Sparkle init
- `Tippi/Core/HotkeyManager.swift` — Carbon `RegisterEventHotKey` (id 1=trigger, 2=dictation, 99=safety)

## Directory map

```
Tippi.xcodeproj/         ← generated by xcodegen, do not edit by hand
Tippi/
  App/                   ← lifecycle, hotkey wiring, Sparkle, menubar
  Core/                  ← business logic (DictationController, HotkeyManager, KeyCombo)
  LLM/                   ← provider implementations + router (one .swift per provider)
  UI/                    ← SwiftUI views (Settings tabs, PromptPopup, Preview, Welcome)
  Voice/                 ← Whisper integration (AudioRecorder, WhisperTranscriber, WhisperConfig)
  Helpers/               ← bundled binaries (whisper-cli source + build artifacts)
  Resources/             ← Info.plist, entitlements, Assets, Localizable.strings (en + de)
scripts/                 ← release.sh (build + sign + notarize + DMG + appcast)
docs/                    ← HANDOVER.md, ONE-PAGER.md, website source for GitHub Pages
dist/                    ← built DMGs (gitignored)
build/                   ← Xcode build artifacts (gitignored)
appcast.xml              ← Sparkle update feed (committed; also pushed to Gist on release)
project.yml              ← XcodeGen definition — single source of truth for VERSION (MARKETING_VERSION)
release.env              ← DEVELOPER_ID / NOTARY_PROFILE only (gitignored; no VERSION)
scripts/bump-version.sh  ← patches project.yml + CHANGELOG stub for new releases
Makefile                 ← convenience wrappers (make build, make release, make open)
```

## Key files (must-understand)

| File | Why |
|---|---|
| `Tippi/App/AppDelegate.swift` | hotkey wiring, menubar, lifecycle. Touch when adding menu items or hotkey behavior |
| `Tippi/Core/DictationController.swift` | dictation state machine (idle → recording → transcribing → insert). Has `DictationSettings` enum at top |
| `Tippi/LLM/LLMRouter.swift` | provider registry + routing. Add new providers to `allProviders` array |
| `Tippi/LLM/ProviderModelPresets.swift` | curated model lists per provider — single source of truth for picker dropdowns |
| `Tippi/LLM/LLMProvider.swift` | protocol all providers conform to |
| `Tippi/UI/SettingsView.swift` | all settings tabs (massive file, ~1300 lines). Tabs are private structs |
| `Tippi/UI/PromptPopup/` | cursor popup for action selection |
| `Tippi/Voice/WhisperTranscriber.swift` | whisper-cli wrapper + WhisperConfig (language, model path) |
| `scripts/release.sh` | release pipeline. Includes drift check that aborts if Help strings don't match provider count |

## Build / run

```bash
cd ~/Coding/Tippi

make open           # opens Xcode for development
make build          # release build, signs with Developer ID, output in build/Build/Products/Release/
make release        # full pipeline: build + notarize + DMG + GitHub Release + Sparkle appcast
```

## Release pipeline gotchas

`release.sh` is strict. It will abort if:

1. **`release.env` must NOT define VERSION** — project.yml MARKETING_VERSION is the single source of truth (since 2026-06-02). `release.sh` aborts if release.env still carries a VERSION. Bump via `./scripts/bump-version.sh X.Y.Z` (patches project.yml + CHANGELOG stub).
2. **CHANGELOG.md release notes missing, empty, or still the placeholder stub** — added 2026-06-05 (Hex-Pattern, fail-fast). `release.sh` extracts the `## [VERSION]` section and aborts if (a) the section is missing, (b) the section body is blank, or (c) the section contains only the bump-version.sh placeholder `- _Add release notes here._`. Fix: write real notes in CHANGELOG.md before `make release`.
3. **Help strings don't mention all providers** — `settings.help.apiBody` (en + de) must list every provider in `LLMRouter.allProviders`. `settings.about.feature2` must match the provider count. When adding a new provider, update both languages.
4. **Sparkle CLI missing** — needs `~/Developer/sparkle-tools/bin/generate_appcast`. If missing, appcast step is silently skipped (auto-updates break).
5. **Notarization not set up** — needs `xcrun notarytool store-credentials tippi-notary`.

## Architecture key decisions (non-obvious)

- **Text capture is AX-first, clipboard-fallback.** `AXUIElementCopyAttributeValue` for native apps. For Electron/Chromium apps (Obsidian, Claude, ChatGPT, Slack, VS Code) AX writes silently fail — fall back to clipboard + `Copy to clipboard` toast. See `Tippi/UI/Preview/PreviewWindowController.swift` and `TextInsertion.replace(...)`.
- **Selection range is captured BEFORE popup appears.** Popup steals focus → selection collapses. `lastSelectionElement` + `lastSelectionRange` stored in `AppDelegate` at trigger time, used to re-select for in-place replace.
- **Hotkey: Carbon `RegisterEventHotKey` + NSEvent global monitor as backup.** Pure NSEvent loses events under certain TCC states. Carbon is the reliable path.
- **Sparkle nested-bundle signing is inside-out.** Re-signing the outer app with `--entitlements` strips Sparkle's XPC signatures. Sign XPC binaries → XPC bundles → Sparkle.framework → outer app, in that order. Implemented in `scripts/release.sh`.
- **`get-task-allow=false` mandatory in entitlements** — Apple rejects notarization otherwise. Xcode default is `true` for Release. Manually re-sign post-build.
- **whisper-cli is bundled, static, Metal-embedded** — built once via `make prepare-binary`, lives in `Tippi/Helpers/whisper-cli` (1.8M, system frameworks only). Lets Tippi ship without any runtime dependency on Homebrew.
- **Dictation post-processing (v1.8.0+) is opt-in + non-fatal** — Whisper transcript → optional LLM polish via `LLMRouter`. On any LLM failure, raw transcript is inserted instead. Default OFF. Skips entirely for utterances < 20 chars.

## Common tasks — file map

| Task | Touch these |
|---|---|
| Add a new LLM provider | new `XProvider.swift` in `Tippi/LLM/` (conform to `LLMProvider`) → register in `LLMRouter.allProviders` → add preset list to `ProviderModelPresets.swift` → add hint string `settings.providers.hint.X` (en + de) → update `settings.help.apiBody` (en + de) → bump provider count in `settings.about.feature2` |
| Add a built-in prompt | append to `DemoPrompt.builtIn` in `DemoPrompt.swift` |
| Change hotkey default | `KeyCombo.dictationDefault` / `KeyCombo.default` in `KeyCombo.swift` |
| Add menubar entry | `setupMenuBar()` in `AppDelegate.swift` |
| Add settings tab | new private struct in `SettingsView.swift`, register in `SettingsView.body` TabView |
| New release | `make bump VERSION=X.Y.Z` (patches project.yml + CHANGELOG stub) → edit CHANGELOG entries → commit → `make release` |

## Anti-footguns

- **Never edit `Localizable.strings` with `cat >>`** — wrong encoding, `plutil -lint` rejects. Use Edit tool or `git checkout HEAD -- file.strings` to recover.
- **Mixed German `„` opener + ASCII `"` closer in `.strings` files breaks parsing** — `"` ends the string literal early. Use typographic `"` or `\"` consistently.
- **Never `cp -R` the build into `/Applications` while Tippi is running** — old TCC grant on stale identity will linger. Quit + sleep 2 + rm + cp.
- **Don't change `temperature` to 0.3 unconditionally for OpenAI** — gpt-5*, o1*, o3*, o4* reject any custom temp. See `OpenAIProvider.isReasoningModel(...)`.
- **`venv` directories aren't portable across Macs** — Shebangs are absolute. Always rebuild on each machine. Never AirDrop `.venv`.

## Vault cross-refs

- Project file: `~/MWs2ndBrain/02 Projekte/Tippi.md` (frontmatter `current_status` + `next_step` = single source of truth for "where are we")
- Skill for vault sync: `/tippi-sync` — drift-check vault ↔ code after release
